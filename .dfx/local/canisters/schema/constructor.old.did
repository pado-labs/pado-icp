type Entry = 
 record {
   context: text;
   description: text;
   name: text;
   schemaString: text;
 };
service : {
  createSchema: (Entry) -> (text);
  getInitializer: () -> (principal) query;
  getSchema: (text) -> (opt Entry) query;
}
